version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: crossreach-db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: crossreach
      POSTGRES_PASSWORD: crossreach_dev
      POSTGRES_DB: crossreach
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U crossreach"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: crossreach-redis
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
